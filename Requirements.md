# OSS-DOCLINTER需求文档

## 需求分析

📈 **具体需求分解如下**：

1. 扫描能力： 工具必须能够扫描开源项目的文档文件，明确提到了README、CONTRIBUTING、Wiki等 。
2. 评估能力： 这是项目的核心。你需要对文档进行两个维度的自动评估：
3. 完整性： 即“必要内容是否齐全” 。
4. 规范性： 即“Markdown 格式、排版等” 。
5. 报告输出： 工具必须能生成一份评估报告，报告中需要包含评分、缺失内容和改进建议 。
6. 前端界面： 这是一个强制要求 (“必须提供”) 。你需要提供一个 Web 或桌面应用作为前端 。
7. 用户交互： 前端界面需要允许用户输入项目 URL ，并且能够展示检测结果 。
8. 验证要求： 你需要用这个工具对至少 1 个真实开源项目进行测试 。

⚙️ **核心功能模块**

基于以上需求，需要实现以下几大核心功能模块：

**1. 项目数据获取模块**
- 功能： 根据用户输入的项目 URL（例如 GitHub 仓库地址），获取用于分析的文档文件。

- 实现思路：

   - 需要能解析不同平台的 URL。

   - 使用 Git 命令（git clone）将项目下载到本地临时目录，或者使用平台 API（如 GitHub API）来抓取特定的文件内容（如 README.md, CONTRIBUTING.md）以及 Wiki 页面的内容。

**2. 文档扫描与解析模块**
- 功能： 在获取的项目数据中，定位并读取所有相关的文档文件。

- 实现思路：

  - 需要扫描文件，在项目根目录及 .github、docs 等常见目录中查找 README (不区分大小写，支持 .md, .rst 等后缀)、CONTRIBUTING.md、CODE_OF_CONDUCT.md 等。

  - 需要解析Markdown（或针对其他格式的解析），对其内容进行评估。

**3. 质量评估引擎**
- 功能 3a (完整性检查)： 检查文档是否包含了“必要内容”。

  - 实现思路： 你需要定义一套规则。例如：

    - README： 是否包含“项目描述”、“安装指南”、“使用示例”、“如何贡献”等章节标题？

    - CONTRIBUTING： 是否包含“如何报告 Bug”、“如何提交 PR”、“开发环境设置”、“代码风格指南”等内容？

- 功能 3b (规范性检查)： 检查 Markdown 格式和排版 。

  - 实现思路： 这可以复用现有的“Linter”工具（如 markdownlint）或自行实现规则。例如：

    - 是否有多余的空行？

    - 标题层级是否正确？

    - 代码块是否标记了语言？

    - 是否存在失效的链接？

**4. 报告与建议生成模块**

- 功能： 汇总评估引擎的检查结果，生成结构化的报告，包括评分、问题和建议 。

- 实现思路：

  - 评分系统： 设计一个评分模型。例如，总分100分，完整性占 60 分，规范性占 40 分。每条规则对应一定分数，未通过则扣分。

  - 缺失内容： 汇总所有在“完整性检查”中未通过的规则，例如：“[缺失] 未找到‘安装指南’章节”。

  - 改进建议： 针对每一个问题，提供具体的修改建议。例如：“[规范性问题] README.md 第 10 行：标题层级错误”，“[改进建议] 请确保标题从 H1 或 H2 开始，并按顺序递增”。

**5. 前端界面 (UI)**

- 功能： 必须提供 Web 或桌面应用界面 。

- 实现思路：

  - 输入区： 一个文本框，允许用户粘贴开源项目的 URL 。


  - 展示区： 用于展示检测结果 。这应该是一个仪表盘，清晰地展示：

    - 总评分。

    - 问题列表：分类（完整性/规范性）展示所有发现的问题。

    - 改进建议：对应每个问题的具体建议 。