## **项目设计文档：开源项目文档质量分析工具 (OSS-DOCLINTER)**

### 1. 项目概述

**1.1. 项目目标**
开发一个Web应用，旨在自动化评估开源项目的文档质量。用户输入一个开源项目仓库的URL，工具将自动分析其核心文档文件（如 `README.md`, `CONTRIBUTING.md` 等），并生成一份包含量化评分、问题诊断和改进建议的综合性报告。此外，工具提供“辅助修复”功能，允许用户通过GitHub授权，一键生成修复后的文档预览，并以用户个人名义发起Pull Request，实现从“发现问题”到“解决问题”的闭环。

**1.2. 解决的问题**
开源项目的文档质量直接影响其可用性、社区参与度和新贡献者的上手门槛。手动评估文档费时费力且标准不一。本项目旨在提供一个快速、客观、标准化的文档质量检测工具，帮助项目维护者改进文档，帮助用户快速评估项目成熟度。多开发者愿意改进文档，但被Fork、Clone、修改、Push、PR等繁琐的Git流程劝退。本工具通过自动化流程式操作，极大降低了文档贡献的门槛。

### 2. 核心需求
*   **功能：** 开发一个可扫描开源项目文档文件（README, CONTRIBUTING, Wiki等）的工具。
*   **评估维度：**
    *   **完整性：** 自动评估必要内容是否齐全（如项目介绍、安装指南、使用示例等）。
    *   **规范性：** 自动评估文档格式与排版是否规范（如Markdown语法、标题层级、代码块等）。
*   **输出：** 生成一份包含评分、缺失内容和具体改进建议的分析报告。
*   **交互：** 必须提供前端界面（Web应用），用户可输入项目URL并直观地查看分析结果。
* 用户认证： 支持 GitHub OAuth 登录，获取用户授权以代表用户执行 Git 操作。
* 新增: 辅助修复与预览： 针对 LLM 提出的改进建议，提供“生成修复”功能。前端需提供 Diff (差异) 视图，让用户在提交前直观对比原文档与修改后的文档。
* 自动化 PR： 用户确认修改后，系统自动在后台完成 Fork、Branch、Commit 和 Pull Request 的全流程。
*   **验证：** 需对至少一个真实的开源项目进行测试，以验证工具的有效性。

### 3. 系统架构设计

本系统采用前后端分离的架构。

*   **前端 (Frontend):** 纯客户端应用，负责用户交互、接收URL输入、向后端发起分析请求以及可视化地展示后端返回的分析报告。
*   **后端 (Backend):** 无状态的API服务，负责处理核心分析逻辑。它接收前端的请求，通过第三方API获取文档内容，执行混合分析（Linter + LLM），并将结果整合成JSON报告返回。负责处理 OAuth 认证流程，安全存储用户临时的 Access Token。除了分析逻辑外，还需实现 Git 的写操作逻辑（Fork, Commit, PR）。
*   **外部服务 (External Services):**
    *   **代码托管平台API (GitHub API、GitLink、Gitee):** 用于替代`git clone`，直接、快速地获取指定文档文件的内容。
    *   **大语言模型API (如 OpenAI API):** 用于进行语义层面的内容完整性分析和建议生成。

### 4. 技术选型

*   **前端:**
    *   框架: **Vue.js** 或 **React** (选择团队熟悉的)
    *   UI库: **Ant Design** 或 **Element Plus** (提供成熟的组件，如输入框、卡片、进度条、标签等，便于快速构建美观的报告界面)
    *   HTTP客户端: **Axios**
    *    vue-diff (用于展示修改前后的差异)

*   **后端:**
    *   语言: **Python **
    *   Web框架: **FastAPI** (性能高，异步支持优秀，自动生成API文档，非常适合I/O密集型任务)
    *   Git平台API交互: **PyGithub** (针对GitHub) 或 **httpx** (通用的异步HTTP客户端)
    *   Markdown规范性分析: **markdownlint-cli** (通过子进程调用) 或 Python库 **pymarkdownlnt**
    *   LLM交互: **openai**
    *   环境管理: **pipenv**
    *   OAuth认证: Authlib 或 FastAPI-SSO (处理 GitHub 登录跳转和 Token 获取)

### 5. 核心工作流程

1.  **用户输入:** 用户在前端界面输入一个GitHub项目URL（例如：`https://github.com/owner/repo`）并点击“分析”。
2.  **前端请求:** 前端向后端API（例如 `POST /api/analyze`）发送请求，请求体中包含该URL。
3.  **后端处理 - 解析与获取:**
    *   后端接收请求，解析URL，提取出 `owner` 和 `repo`。
    *   后端调用GitHub API，首先获取仓库根目录的文件列表，以确定`README`文件的确切名称（可能是`README.md`, `README.rst`等）。
    *   并行地向GitHub API发送请求，获取`README.md`、`CONTRIBUTING.md`等目标文件的内容（Base64编码，需解码为字符串）。
4.  **后端处理 - 并行分析:**
    *   **任务A (规范性分析):** 将获取到的Markdown文件内容传入 **Markdown Linter**。Linter会立即返回一个结构化的列表，包含所有格式问题（如：标题层级错误、列表格式不当、行尾多余空格等）。此过程速度极快。
    *   **任务B (完整性分析):** 将文件内容与预设的 **Prompt** (见 7.2 节) 一同发送给 **LLM API**。LLM将根据评分标准进行语义理解，判断是否包含项目描述、安装指南、API文档等关键部分，并生成评分和改进建议。此过程是主要的时间开销。
5.  **后端处理 - 报告汇总:**
    *   后端等待任务A和任务B全部完成。
    *   将Linter的格式问题报告和LLM的内容完整性报告，按照预定义的JSON结构（见 6. 节）进行整合，计算出总分。
6.  **响应与展示:**
    *   后端将最终的JSON报告作为响应返回给前端。
    *   前端接收到JSON数据，根据其内容动态渲染出一个可视化的报告页面，清晰地展示总分、各项得分、存在的问题和改进建议。
7.  **用户登录:**
    用户点击“登录并修复”，跳转至 GitHub OAuth 授权页面，授权后返回应用，后端获取用户的 Access Token。
8.  **生成修复内容:**
    用户针对某个缺失项（如“缺失安装指南”）点击“生成修复”。
    后端调用 LLM，基于现有文档上下文，生成该章节的具体 Markdown 内容，并将其插入原文档，形成新的完整文档字符串。
9.  **Diff 预览与确认:**
    前端接收新旧文档内容，使用 Diff 组件左右对照展示。
    用户人工审查修改内容，可进行简单的文本微调，确认无误后点击“提交 PR”。
10. **自动化 PR 提交 (后端执行):**
    Check & Fork: 检查用户账号下是否已存在该仓库的 Fork。若无，调用 API 进行 Fork。
    Create Branch: 在用户的 Fork 仓库中创建一个新分支 (如 docs/fix-readme-completeness)。
    Commit: 将修改后的文件内容直接通过 API 提交到该分支。
    Create PR: 调用 API，从用户的 Fork 分支向源仓库的主分支发起 Pull Request。
11. **反馈:** 
    前端提示“PR 创建成功”，并提供跳转到 GitHub PR 页面的链接。

### 6. API接口与数据模型

**API Endpoint:** `POST /api/analyze`

**Request Body:**
```json
{
  "url": "https://github.com/fastapi/fastapi"
}
```

**Response Body (最终报告JSON结构示例):**
```json
{
  "repository": "fastapi/fastapi",
  "overall_score": 85,
  "report_time": "2023-10-27T10:00:00Z",
  "completeness_check": { // 来自LLM
    "score": 80,
    "details": [
      {
        "check_item": "Project Description",
        "passed": true,
        "score": 10,
        "suggestion": "项目描述清晰，很好地阐述了项目目标。"
      },
      {
        "check_item": "Installation Guide",
        "passed": true,
        "score": 20,
        "suggestion": "提供了详细的pip安装命令。"
      },
      {
        "check_item": "Usage Example",
        "passed": false,
        "score": 0,
        "suggestion": "建议在README开头部分增加一个'Hello World'级别的快速上手代码示例，能让新用户更快理解其核心用法。"
      }
      // ... 其他完整性检查项
    ]
  },
  "format_check": { // 来自Linter
    "score": 90,
    "details": [
      {
        "rule_id": "MD001",
        "description": "Header levels should only increment by one level at a time",
        "line_number": 42,
        "suggestion": "标题层级错误：在H2标题后直接使用了H4标题，请检查文档结构。"
      }
      // ... 其他格式问题
    ]
  }
}
```

GET /api/auth/login: 重定向到 GitHub 登录。
GET /api/auth/callback: 处理 GitHub 回调，交换 Token。
POST /api/fix/generate: 接收文档 ID 和问题 ID，让 LLM 生成修复后的全文。
POST /api/fix/pr: 接收用户确认后的文档内容，执行 PR 提交逻辑。

### 7. 分析引擎核心设计

**7.1. 评分与评估标准**

我们将评估标准分为两大类，并为每个检查项分配权重。

| 维度       | 检查项                     | 分数权重 | 分析工具           |
| :--------- | :------------------------- | :------- | :----------------- |
| **完整性** | 项目简介与目标             | 10       | LLM                |
|            | 安装与依赖指南             | 20       | LLM                |
|            | 快速上手/使用示例          | 20       | LLM                |
|            | 贡献指南 (CONTRIBUTING)    | 15       | LLM                |
|            | 行为准则 (Code of Conduct) | 5        | LLM/文件存在性检查 |
|            | 许可证信息 (License)       | 5        | LLM/文件存在性检查 |
| **规范性** | Markdown语法正确性         | 10       | Linter             |
|            | 标题层级与结构             | 5        | Linter             |
|            | 代码块语言标识             | 5        | Linter             |
|            | 链接有效性 (可选高级功能)  | 5        | Linter              |
| **总分**   |                            | **100**  |                    |

**7.2. LLM Prompt工程示例**

这是驱动完整性分析的核心。Prompt的设计至关重要，需要明确指示LLM扮演的角色、遵循的规则以及输出的格式。

```text
# ROLE
You are an expert Open Source Project Documentation Quality Analyst.

# TASK
Analyze the provided README.md file content and evaluate its completeness based on a predefined checklist. You must provide a score and a constructive suggestion for each item.

# CHECKLIST & SCORING (Total 75 points)
1.  **Project Description (10 points):** Does it clearly state what the project is and what problem it solves?
2.  **Installation Guide (20 points):** Are there clear, copy-pasteable instructions on how to install the project and its dependencies?
3.  **Usage Example (20 points):** Is there a simple "Hello World" or quick-start code example that demonstrates the project's core functionality?
4.  **Contribution Guide (15 points):** Does it mention how others can contribute, or link to a CONTRIBUTING.md file?
5.  **Code of Conduct (5 points):** Is a Code of Conduct mentioned or linked?
6.  **License Information (5 points):** Is the project's license clearly stated or linked?

# INPUT
<PASTE README CONTENT HERE>

# OUTPUT FORMAT
You MUST respond with a valid JSON array of objects, where each object represents an item from the checklist. Do not include any text outside of the JSON. The structure for each object is:
{
  "check_item": "Name of the item from the checklist",
  "passed": boolean, // true if the content is present and clear, false otherwise
  "score": integer, // Score awarded for this item (0 if not passed)
  "suggestion": "A concise, one-sentence constructive suggestion for improvement. If it passed, provide positive feedback."
}
```

### 8. 风险与缓解措施

*   **风险：** 第三方API速率限制（GitHub, OpenAI）。
    *   **缓解：** 使用认证的API Token提高速率限制。在后端实现缓存机制，对同一URL在短时间内的重复请求直接返回缓存结果。
*   **风险：** LLM API成本和延迟。
    *   **缓解：** 优先选择成本效益高的模型（如GPT-3.5-Turbo）。优化Prompt以减少Token消耗。对长文档进行适当截断或摘要处理。在前端设置合理的超时和加载提示。
*   **风险：** 不支持的平台或文件格式（如 GitLab, Gitee, `.rst`文件）。
    *   **缓解：** 初期明确声明仅支持GitHub和Markdown文件。在URL解析阶段进行判断，对不支持的URL给出友好提示。未来可将数据获取和文件解析部分模块化，以便扩展支持其他平台和格式。
*   **风险：** LLM输出格式不稳定。
    *   **缓解：** 在Prompt中强制要求JSON输出，并提供清晰的格式示例。在后端对LLM的响应进行严格的JSON解析和数据校验，如果解析失败，可以进行重试或返回一个特定的错误信息。
